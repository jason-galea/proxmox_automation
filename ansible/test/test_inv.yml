all:
  # hosts:
  #   mail.example.com:
  children:
    test_hosts:
      hosts:
        apple:
          id: 1
          # user: user1
          # ansible_host: "{{ network ~ id }}"
        banana:
          id: 2
          # user: user2
          # ansible_host: "{{ network ~ id }}"
        cranberry:
          id: 3
          # user: user3
          # ansible_host: "{{ network ~ id }}"
      vars:
        network: 172.26.0.
        user: "{{ inventory_hostname }}"
        ansible_host: "{{ network ~ id }}"
          
